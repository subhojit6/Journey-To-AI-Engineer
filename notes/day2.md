q. What is OOP?
ans. OOP's are nothing but object oriented programming where we create classes as objects with methods inside those.

q. What is a class?
ans. A class is like a user defined blueprint or template used to create objects. Basically class is keyword which is used to define a class, it bundles data known as attributes and functionality known as methods.

q. What is object?
ans. A object in python is a self contained unit that bundles data and functionality together.

q. Why self exits?
ans. self exists to represent the specific instance af a class so that methods can access and modify the unique object's attributes and behaviour.

q. Difference between write and append?
ans. write overwrite the file, whereas the append continues without overwriting the file. 

q. Why persistence matters?
ans. ohhh, persistance is vital ability to keep moving forward despite setbacks. It bridges the gap between raw potential and actual achievement.