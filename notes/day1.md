q. What is venv?
ans. venv is nothing but a virtual environment created to maitain and have notes of the packages with specific version is needed to run the code without any errors

q. Why return?
ans. return keyword is used to return a value from the function which can be used furthur and if it is in a look it breaks and return a value.

q. Difference between parameter and argument?
ans. parameter is something which is used while creating a function to get the data as an argument which is passed from outside the function

q. Why dictionary?
ans. To save the data in key value pairs.

q. Why use functions?
ans. To avoid code repetation. Instead a function can be called multiple times.
